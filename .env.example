########################################
# ProductForge Backend - Env Template  #
# (Local dev vs Railway production)    #
########################################

# --- OpenAI ---
# Required in both local and Railway (set the real key in Railway Project Variables).
OPENAI_API_KEY=your-openai-api-key

# --- Redis ---
# Local development
REDIS_URL=redis://localhost:6379

# Railway production (example - replace HOST and PASSWORD from Railway Redis service)
# Tip: Railway usually provides a full connection string or separate HOST/PASSWORD.
# Use the full URL below format; username must be 'default'.
# REDIS_URL=redis://default:YOUR_PASSWORD@YOUR_HOST:6379

# --- Server ---
# Railway injects PORT automatically; you generally don't need to set it there.
# Keep local fallback here for running locally.
PORT=8000
MAX_UPLOAD_SIZE=10485760

# --- Logging ---
LOG_LEVEL=INFO

# --- Health / Worker ---
HEALTH_CHECK_INTERVAL=30
WORKER_RESTART_DELAY=5

# --- Uploads ---
UPLOAD_TIMEOUT=300

# --- Notes ---
# RAILWAY_ENVIRONMENT is set automatically by Railway. You don't need to define it.
# The app uses /tmp/logs on Railway for logs and workspace/logs locally.
